# liquibase/changelogs/db.changelog-master.yaml
databaseChangeLog:
  - include: { file: tables/person.yaml }
  - include: { file: tables/product.yaml }
  - include: { file: tables/order.yaml }

  # Auditoria (apenas cria tabela; inserts são feitos pelo script bash)
  - include: { file: util/audit.yaml }

  # Reset (context=reset)
  - include: { file: util/reset.yaml }
